apiVersion: v1
kind: Service
metadata:
  name: trend-app-service
  namespace: trend-app
spec:
  type: NodePort
  selector:
    app: trend-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30080
